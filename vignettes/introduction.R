## -----------------------------------------------------------------------------
library(EpiBayes)
data("EuropeCovid")

## -----------------------------------------------------------------------------
names(EuropeCovid)

## -----------------------------------------------------------------------------
args <- EuropeCovid
data <- args$data
head(data)

## -----------------------------------------------------------------------------
deaths <- args$obs$deaths
names(deaths)

## -----------------------------------------------------------------------------
w <- data$country %in% "United_Kingdom"
data <- data[w,]
args$data <- data

## -----------------------------------------------------------------------------
args$formula <- R(country, date) ~ 0 + lockdown 

